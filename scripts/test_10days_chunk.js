const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function run() { const matches = await prisma.$queryRaw`SELECT t.id FROM SquadMatchTelemetry t JOIN SquadMatch m ON t.squadMatchId = m.id WHERE t.status = "SUCCESS" AND m.createdAt >= DATE_SUB(NOW(), INTERVAL 10 DAY)`; console.log(`Found ${matches.length} matches for the last 10 days.`); let count = 0; for (const match of matches) { try { await prisma.squadMatchTelemetry.update({ where: { id: match.id }, data: { status: "PENDING" } }); count++; } catch(e){} } console.log(`Updated ${count} matches successfully without locking the whole table.`); } run().catch(console.error).finally(() => prisma.$disconnect());
