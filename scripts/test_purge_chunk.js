const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function run() { let affected = 1; let total = 0; while (affected > 0) { const res = await prisma.$executeRaw`UPDATE SquadMatchTelemetry SET positionSamples = NULL, trajectorySegments = NULL WHERE positionSamples IS NOT NULL LIMIT 100`; affected = res; total += res; console.log("Purged chunk:", res, "Total:", total); } console.log("Done. Total purged:", total); } run().catch(console.error).finally(() => prisma.$disconnect());
