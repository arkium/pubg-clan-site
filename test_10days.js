const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function run() { const result = await prisma.$executeRaw`UPDATE SquadMatchTelemetry t JOIN SquadMatch m ON t.squadMatchId = m.id SET t.status = "PENDING" WHERE t.status = "SUCCESS" AND m.createdAt >= DATE_SUB(NOW(), INTERVAL 10 DAY)`; console.log("Updated", result, "matches to PENDING"); } run().catch(console.error).finally(() => prisma.$disconnect());
