const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function run() { try { const res = await prisma.$executeRaw`UPDATE SquadMatchTelemetry SET positionSamples = NULL, trajectorySegments = NULL LIMIT 10`; console.log("Rows affected:", res); } catch(err) { console.error("Error:", err); } } run().catch(console.error).finally(() => prisma.$disconnect());
