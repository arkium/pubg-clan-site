const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function run() { const matches = await prisma.squadMatchTelemetry.findMany({ select: { memberStats: true } }); let recallCount = 0; for (const match of matches) { if (!match.memberStats) continue; const stats = Array.isArray(match.memberStats) ? match.memberStats : JSON.parse(match.memberStats); for (const member of stats) { if (member.recalls > 0) recallCount += member.recalls; } } console.log("Total recalls in all parsed matches:", recallCount); } run().catch(console.error).finally(() => prisma.$disconnect());
